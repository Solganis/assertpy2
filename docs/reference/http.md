# HTTP response assertions

The step from an HTTP response into its parsed body. Duck-typed on the shape every client keeps, so no
client library is imported and none has to be installed.

::: assertpy2.http_mixin.HttpMixin
